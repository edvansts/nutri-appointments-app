import { colors } from '@styles/theme';
import styled from 'styled-components/native';

export const NutritionistInfoContainer = styled.View`
  background-color: ${colors.grayLighter};
  padding: 38px 16px 20px;
`;
